flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.26.0
scipy>=1.11.0
python-dotenv>=1.0.0

# Speech-to-text (Whisper variant)
faster-whisper>=1.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Acoustic models — wav2vec2, Whisper, BLIP
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0
pillow>=10.0.0

# YamNet via TensorFlow Hub
tensorflow>=2.15.0
tensorflow-hub>=0.16.0

# Face landmark + pose
mediapipe>=0.10.10
opencv-python>=4.8.0

# Speaker diarization (optional, requires pyannote token)
# pyannote.audio>=3.1.0

# LLM via Groq
groq>=0.5.0

# Dataset download
requests>=2.31.0
tqdm>=4.66.0

# YOLOv11 hazard detector
ultralytics>=8.3.0

# Visual emotion (DeepFace — Ekman 7-class)
deepface>=0.0.93
tf-keras>=2.15.0

# HTTPS certificate bundle — needed so the YamNet model download can verify
# the TF-Hub certificate on macOS, which otherwise silently falls back to a
# weaker embedding (see the evaluation chapter).
certifi>=2024.2.2

# HuggingFace hub client (used directly for model resolution/downloads)
huggingface-hub>=0.23.0

# Text-to-speech fallback when no neural provider is configured
gtts>=2.5.0

# Misc
scikit-learn>=1.3.0


# ─── Development and evaluation only ───────────────────────────────
# Not required to run the assistant; required to run the test suite and
# the evaluation scripts in backend/scripts/.
pytest>=8.0.0
matplotlib>=3.8.0
